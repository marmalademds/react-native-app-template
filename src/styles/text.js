import { StyleSheet } from "react-native";

export const text = StyleSheet.create({
    fboldheader: {
        fontSize: 30,
        fontWeight: 'bold'
    },
    fboldsmall: {
        fontSize: 14,
        fontWeight: 'bold'
    },
    fsmall: {
        fontSize: 14,
    },
    buttonText: {
        color: 'white',
    },
    flinksmall: {
        fontSize: 14,
        color: 'blue',
        fontWeight: 'bold',
        textDecorationLine: 'underline'
    }
})